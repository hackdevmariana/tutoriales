import{_ as e,o as a,c as o,d as r}from"./app-DELkFWxI.js";const n={},d=r('<h1 id="sistema-de-rutas-de-nuxt" tabindex="-1"><a class="header-anchor" href="#sistema-de-rutas-de-nuxt"><span>Sistema de rutas de Nuxt</span></a></h1><p>Las rutas en <code>Nuxt</code> son la forma de definir y acceder a las diferentes páginas de una aplicación web. <code>Nuxt</code> utiliza el sistema de enrutamiento de <code>vue-router</code>, pero lo simplifica y mejora con algunas características adicionales.</p><p>Algunas de estas características son:</p><ul><li>El enrutamiento basado en el sistema de archivos, que crea automáticamente las rutas a partir de los archivos que creamos en el directorio <code>pages/</code> de un proyecto.</li></ul><p>Por ejemplo, si creamos un archivo <code>index.vue</code>, tendremos una ruta <code>/</code> que mostrará el contenido de ese archivo. Si creamos un archivo <code>about.vue</code>, tendremos una ruta <code>/about</code> que mostrará el contenido de ese archivo. Y así sucesivamente.</p><ul><li>El enrutamiento dinámico, que permite crear rutas que dependen de parámetros variables, como el id de un producto, el nombre de un usuario, etc. Para crear una ruta dinámica, sólo tenemos que nombrar el archivo con un guión bajo (_) seguido del nombre del parámetro.</li></ul><p>Por ejemplo, si creamos un archivo <code>_id.vue</code> dentro del directorio <code>products/</code>, tendremos una ruta <code>/products/:id</code> que mostrará el contenido de ese archivo según el valor de id que se pase en la URL.</p><ul><li>El enrutamiento anidado, que permite crear rutas que contienen subrutas dentro de una ruta principal. Para crear una ruta anidada, solo tenemos que crear un directorio con el nombre de la ruta principal y poner dentro los archivos de las subrutas.</li></ul><p>Por ejemplo, si creamos un directorio <code>users/</code> y dentro ponemos un archivo <code>index.vue</code> y otro <code>_id.vue</code>, tendremos una ruta <code>/users</code> que mostrará el contenido de <code>index.vue</code> y una ruta <code>/users/:id</code> que mostrará el contenido de <code>_id.vue</code>.</p><ul><li>El enrutamiento con comodines, que permite crear rutas que coincidan con cualquier valor que se pase en la URL. Para crear una ruta con comodines, sólo tenemos que nombrar el archivo con un asterisco (*) seguido del nombre del parámetro.</li></ul><p>Por ejemplo, si creamos un archivo <code>*.vue</code> en el directorio <code>pages/</code>, tendremos una ruta <code>/:pathMatch</code> que mostrará el contenido de ese archivo según el valor de <code>pathMatch</code> que se pase en la URL.</p><p>El pathMatch es una propiedad que se usa en el enrutamiento de Nuxt para crear rutas con comodines, que coinciden con cualquier valor que se pase en la URL. Para crear una ruta con comodines, solo tenemos que nombrar el archivo con un asterisco (*) seguido del nombre del parámetro.</p><p>Esta característica es útil para crear páginas de error personalizadas, o para manejar rutas que no existen o que no se pueden definir de antemano. Sin embargo, debemos tener en cuenta que las rutas con comodines tienen la menor prioridad, y solo se activan si ninguna otra ruta coincide con la URL.</p><p>Además, debemos usar el método <code>$route.params.pathMatch</code> para acceder al valor del parámetro en el componente.</p><ul><li>El middleware de rutas, que te permite ejecutar una función antes de entrar en una ruta, para hacer alguna validación, autenticación, redirección, etc. Para crear un middleware de rutas, solo tenemos que crear un archivo con el nombre del middleware en el directorio <code>middleware/</code> del proyecto y asignarlo a la propiedad middleware de la página que queramos proteger.</li></ul>',15),c=[d];function t(s,i){return a(),o("div",null,c)}const l=e(n,[["render",t],["__file","rutas.html.vue"]]),m=JSON.parse('{"path":"/nuxt/rutas.html","title":"Sistema de rutas de Nuxt","lang":"en-US","frontmatter":{},"headers":[],"git":{"updatedTime":1709428973000,"contributors":[{"name":"fernando","email":"fernando@juandemariana.org","commits":1}]},"filePathRelative":"nuxt/rutas.md"}');export{l as comp,m as data};
