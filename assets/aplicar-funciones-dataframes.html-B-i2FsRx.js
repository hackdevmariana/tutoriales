import{_ as a,o as n,c as e,d as s}from"./app-B6AS8mW0.js";const o={},c=s(`<h1 id="aplicar-funciones-en-dataframes" tabindex="-1"><a class="header-anchor" href="#aplicar-funciones-en-dataframes"><span>Aplicar funciones en dataframes</span></a></h1><p>Para aplicar una función a todos los elementos de un dataframe en R, podemos utilizar la función <code>apply()</code>. Esta función permite aplicar una función a las filas o columnas de un dataframe.</p><p>Un ejemplo de cómo usar <code>apply()</code> para aplicar una función a cada elemento de un dataframe:</p><p>Crear un dataframe de ejemplo:</p><div class="language-r line-numbers-mode" data-ext="r" data-title="r"><pre class="language-r"><code>df <span class="token operator">&lt;-</span> data.frame<span class="token punctuation">(</span>a <span class="token operator">=</span> <span class="token number">1</span><span class="token operator">:</span><span class="token number">3</span><span class="token punctuation">,</span> b <span class="token operator">=</span> <span class="token number">4</span><span class="token operator">:</span><span class="token number">6</span><span class="token punctuation">,</span> c <span class="token operator">=</span> <span class="token number">7</span><span class="token operator">:</span><span class="token number">9</span><span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Definir una función para aplicar, por ejemplo, elevar al cuadrado cada elemento:</p><div class="language-r line-numbers-mode" data-ext="r" data-title="r"><pre class="language-r"><code>mi_funcion <span class="token operator">&lt;-</span> <span class="token keyword">function</span><span class="token punctuation">(</span>x<span class="token punctuation">)</span> <span class="token punctuation">{</span>
  x<span class="token operator">^</span><span class="token number">2</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Aplicar la función a cada elemento del dataframe:</p><div class="language-r line-numbers-mode" data-ext="r" data-title="r"><pre class="language-r"><code>df_modificado <span class="token operator">&lt;-</span> apply<span class="token punctuation">(</span>df<span class="token punctuation">,</span> c<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span><span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">,</span> mi_funcion<span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Mostrar el dataframe modificado:</p><div class="language-r line-numbers-mode" data-ext="r" data-title="r"><pre class="language-r"><code>print<span class="token punctuation">(</span>df_modificado<span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>En este código, <code>apply(df, c(1,2), mi_funcion)</code> aplica <code>mi_funcion</code> a cada elemento del dataframe <code>df</code>. El segundo argumento, <code>c(1,2)</code>, indica que la función debe aplicarse tanto a las filas <code>(1)</code> como a las columnas <code>(2)</code>. El resultado es un nuevo dataframe <code>df_modificado</code> con la función aplicada a cada elemento.</p><p>Si solo queremos aplicar la función a las filas o a las columnas, podemos usar 1 o 2 respectivamente en lugar de <code>c(1,2)</code>.</p><p>Además de <code>apply()</code>, existen otras funciones como <code>lapply()</code>, <code>sapply()</code>, <code>vapply()</code>, <code>tapply()</code> y <code>mapply()</code> que también permiten aplicar funciones a estructuras de datos en R de manera eficiente.</p>`,14),t=[c];function p(d,l){return n(),e("div",null,t)}const r=a(o,[["render",p],["__file","aplicar-funciones-dataframes.html.vue"]]),u=JSON.parse('{"path":"/r/aplicar-funciones-dataframes.html","title":"Aplicar funciones en dataframes","lang":"en-US","frontmatter":{},"headers":[],"git":{"updatedTime":1715763058000,"contributors":[{"name":"fernando","email":"fernando@juandemariana.org","commits":1}]},"filePathRelative":"r/aplicar-funciones-dataframes.md"}');export{r as comp,u as data};
