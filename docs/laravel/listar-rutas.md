# Listar las rutas de un proyecto

Para listar las rutas de un proyecto con Artisan, usaremos el comando:

``` sh
php artisan route:list
```

Nos devolverá tanto la ruta como el verbo de acceso:

```
GET|HEAD   admin/login
```
